import React from "react";

function Header() {
    return(
        <nav className="text-center">
            <h1>Employee Directory</h1>
            <br></br>
            <p>Click on name or index headings to sort, or use the search box to narrow results</p>
        </nav>
    );
}

export default Header;